_site/*
.jekyll-metadata

# SSH keys and other secrets — never commit these
id_rsa
id_dsa
id_ecdsa
id_ed25519
*.pem
*.key
*.ppk
*.pub
*_rsa
*_ed25519
.ssh/

# Machine-local Claude instructions (this computer only)
CLAUDE.local.md
_config.local.yml
.dev-serve.sh
a.output
brand/
